/**
 * chathider.inc
 * Native: SetChatStatus(playerid, bool:status)
 * Пока пустая — резерв для будущей логики скрытия чата
 */

#if defined _chathider_included
  #endinput
#endif
#define _chathider_included

native SetChatStatus(playerid, bool:status);
